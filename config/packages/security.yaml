security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'
    
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\User
                property: username

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            lazy: true
            provider: app_user_provider
            custom_authenticator: App\Security\LoginAuthenticator
            logout:
                path: app_logout
                # where to redirect after logout
                # target: app_any_route

            remember_me:
                secret: '%kernel.secret%'
                lifetime: 604800
                path: /
                always_remember_me: true

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#the-firewall

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
    #- { path: ^/admin, roles: ROLE_ADMIN }  # Only allow admins to access /admin routes
    #- { path: ^/user, roles: ROLE_USER }    # Only allow users to access /user routes
    - { path: ^/login, roles: PUBLIC_ACCESS }
    - { path: ^/register, roles: PUBLIC_ACCESS }
    - { path: ^/logout, roles: PUBLIC_ACCESS }
    - { path: ^/tour, roles: ROLE_ADMIN }
    - { path: ^/site, roles: PUBLIC_ACCESS }
    - { path: ^/user, roles: ROLE_USER }
    - { path: ^/settings, roles: ROLE_ADMIN }
    - { path: ^/home, roles: PUBLIC_ACCESS }
    - { path: ^/exhibition, roles: ROLE_ADMIN }
    - { path: ^/dashboard, roles: ROLE_ADMIN}
    - { path: ^/artist, roles: ROLE_ADMIN }
    - { path: ^/admin, roles: ROLE_ADMIN }
    - { path: ^/activity_log, roles: ROLE_ADMIN}

when@test:
    security:
        password_hashers:
            App\Entity\User:
                algorithm: auto
            # Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
